{$FRONTEND_URL:localhost} {
    tls internal

    handle /api/* {
        reverse_proxy backend:3000
    }

    handle {
        root * /srv
        try_files {path} /index.html
        file_server
    }
}
