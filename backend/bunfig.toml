telemetry = false

[install]
# Package should be at least a week old to prevent supply chain vulnerabilities
minimumReleaseAge = 604800  # 3600 × 24 × 7 = 604800

[test]
preload = ["./test/setup.ts"]
coveragePathIgnorePatterns = ["**/test/**", "**/*.test.ts"]
