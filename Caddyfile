{$DOMAIN:localhost} {
    @backend path /api/* /auth/* /projects/* /collab*
    handle @backend {
        reverse_proxy backend:3000
    }

    handle {
        root * /srv
        try_files {path} /index.html
        file_server
    }
}
