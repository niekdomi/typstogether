# Copy to `.env` only if you want OAuth login in local dev.
# Tests load `.env.test` automatically via NODE_ENV=test.

# OAuth providers — leave a pair blank to disable that provider.
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITLAB_CLIENT_ID=
GITLAB_CLIENT_SECRET=
